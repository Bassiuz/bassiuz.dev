name: bassiuz_dev
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^6.3.0
  fluid_ui_design:
    git:
      url: git@gitlab.dcc.tools:niels/fluid-ui-design.git
      ref: feature/convert-to-package # branch name
  google_fonts: ^4.0.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^6.2.0
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3

flutter:
  uses-material-design: true